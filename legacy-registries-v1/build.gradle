archivesBaseName = "legacy-registries-v1"
version = getSubprojectVersion(project, "1.0.0")

dependencies {
	compile project(path: ':legacy-api-base')
	compile project(path: ':legacy-networking-v0')
}
