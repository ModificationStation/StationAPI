archivesBaseName = "station-tool-api-v0"
version = getSubprojectVersion(project, "1.0.0")

moduleDependencies(project, [
        'station-api-base',
        'station-items-v0',
        'station-registry-api-v0'
])